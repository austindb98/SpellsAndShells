using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class AttackWhirl : AttackGust
{
    /*protected override void Update()
    {
        if (canMove)
        {
            //Vector3 movement = 
            transform.Translate(transform.localRotation * Vector3.down * speed * Time.deltaTime, gameObject.transform.parent);
        }
    }*///im stupid
}
